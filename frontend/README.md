# Frontend

This directory contains the web UI rendered inside the Wails desktop shell.

Current scaffold:

- app bootstrap entrypoints
- shell layouts
- feature route stubs
- typed API client and DTO contracts
- runtime binding and realtime adapter contracts

The implementation is intentionally polling-first until backend event publishing is wired end-to-end.
